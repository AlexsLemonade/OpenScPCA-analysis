This directory contains reference files.

* `SCPCP000023-osteocar-samples.tsv` is a TSV of samples in `SCPCP000023` which also appear in the `OsteoCar` reference
* The following files are present for analysis but not included in version control:
  * `patient_mets.qs2`
  * `patient_prim.qs2`
  * `xeno_mets.qs2`
  * `xeno_prim.qs2`
